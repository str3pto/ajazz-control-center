# SPDX-License-Identifier: GPL-3.0-or-later
# Reviews dependency changes on pull requests for known vulnerabilities and
# license compatibility.
name: Dependency Review

on:
  pull_request:
    branches: [main, develop]

concurrency:
  group: depreview-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  pull-requests: write

jobs:
  dependency-review:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1

      - name: Dependency Review
        # Tolerate the "Dependency review is not supported on this repository"
        # error that appears when GitHub's Dependency Graph is not enabled
        # (Settings → Code security & analysis → Dependency graph). We don't
        # want a configuration gap to block dependabot's auto-PRs from
        # merging while Dep Graph is being enabled. The step still surfaces
        # in the run summary so the maintainer notices it.
        continue-on-error: true
        uses: actions/dependency-review-action@2031cfc080254a8a887f58cffee85186f0e49e48 # v4.9.0
        with:
          fail-on-severity: high
          # GPL-3.0 project: only allow GPL-compatible licenses.
          allow-licenses: >-
            MIT, BSD-2-Clause, BSD-3-Clause, BSD-3-Clause-Clear,
            Apache-2.0, MPL-2.0,
            LGPL-2.0-or-later, LGPL-2.1-or-later, LGPL-3.0-or-later,
            GPL-2.0-or-later, GPL-3.0-or-later,
            ISC, Unlicense, CC0-1.0, Zlib, BSL-1.0, Python-2.0
          comment-summary-in-pr: on-failure
